name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: swiftc Core.swift CoreTests.swift -o /tmp/router-tests && /tmp/router-tests
      - run: ./build.sh
